interface:
  display_name: "Ponytail Audit"
  short_description: "Audit a repository for removable complexity"
  default_prompt: "Use $ponytail-audit to audit the whole repository for over-engineering and return a ranked deletion or simplification list without applying changes."
